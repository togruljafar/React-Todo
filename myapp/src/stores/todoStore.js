import { action, observable } from 'mobx';

export default class TodoStore {
    @observable todos = [];
    @action addTodo = () => {

    }

    @action getTodos = () => {

    }

    @action removeTodo = () => {
        
    }
}